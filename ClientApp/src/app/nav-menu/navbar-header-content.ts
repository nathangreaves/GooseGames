export interface INavbarHeaderComponent {
//To hook this up, inherit from this class, create a new enum value in navbar-header.ts
//Then register your new component in module entryComponents in app.module.ts
//Then add it to the collection of components in services/navbar.ts
}
